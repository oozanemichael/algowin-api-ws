#rescu.http.readTimeoutMillis = 30000             # Read timeout in milliseconds when performing HTTP requests. The default is 30000 (30 seconds).
# Proxy host. Both host and port must be set in order to use a proxy.
rescu.http.readProxyHost=127.0.0.1
# Proxy port. Both host and port must be set in order to use a proxy.
rescu.http.readProxyPort=7890
#rescu.http.readProxyType = SOCKS                # Proxy type. Applied only when readProxyHost and readProxyPort are set. Allowed values: DIRECT, HTTP, SOCKS.
#rescu.http.ignoreErrorCodes = true              # If set to true, the HTTP response body never be parsed as Exception but always as the method response type. Defaults to false.